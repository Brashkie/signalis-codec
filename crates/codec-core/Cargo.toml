[package]
name = "codec-core"
description = "Safe, dependency-free protobuf wire codec (Level 1)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["rlib"]

# No dependencies — this is a pure, self-contained wire codec.
[dependencies]
