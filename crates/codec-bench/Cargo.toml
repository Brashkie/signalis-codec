[package]
name = "codec-bench"
version = "0.0.0"
edition = "2021"
publish = false
description = "Criterion benchmarks for signalis-codec (not published)"
license = "Apache-2.0"

[dependencies]
codec-core = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "encode"
harness = false

[[bench]]
name = "decode"
harness = false

[[bench]]
name = "packed"
harness = false
